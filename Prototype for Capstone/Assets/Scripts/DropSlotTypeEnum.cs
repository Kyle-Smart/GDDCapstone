using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DropSlotTypeEnum : MonoBehaviour
{
    public enum DropSlotType {
        GENERATOR,
        BUTTON,
        DOOR,
        DEFAULT
    }
}
